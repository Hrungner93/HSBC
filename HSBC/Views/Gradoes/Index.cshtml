@model IEnumerable<HSBC.Models.Grado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Desripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Desripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Grado1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.Grado1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Grado1 })
        </td>
    </tr>
}

</table>
